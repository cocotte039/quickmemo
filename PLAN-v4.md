# PLAN-v4: コピー改善 / 編集操作の修正 / 検索

## 目的

「書く → コピーして他アプリに貼る」という主用途に沿って、コピーの粒度を選べるようにする。あわせて編集操作の取り消しが効かない不具合を直し、増えたメモを探せるようにする。

## 受入条件

1. ツールバー操作や Enter のリスト自動継続の後でも、ブラウザ標準の取り消し（Ctrl+Z / iOS シェイク）が効く
2. `-` キーの連続押しで `- ` → `- [ ] ` → 解除 と切り替わる
3. Enter で番号付きリスト（`1.`）とチェックボックス（`- [ ]`）が自動継続する
4. コピーボタンのタップでタイトル込み全文がコピーされ、長押しで形式を選べる
5. 「このブロック」でカーソル位置を含む `---` 区切りの範囲だけコピーできる
6. ヘッダーの検索アイコンから、3バケット横断のインクリメンタル検索ができる
7. 既存のメモデータ形式（`version: 2`）は変更しない

## 非目標

- Markdown プレビュー表示
- Share / Share Target（前回の選択で除外）
- 検索結果からのスワイプ操作（結果はバケット混在のため意味が曖昧になる。タップで開くのみ）
- タグ・並べ替えUI

---

## 1. 取り消し（Undo）の修正

### 現状の問題

`handleMarkdownInsert()`（`app.js:1343-1440`）と Enter のリスト自動継続（`app.js:1474-1479`）は、すべて `ta.value = ...` で textarea の値を直接書き換えている。この方式はブラウザが保持する取り消し履歴を破棄するため、ツールバーを押した直後は Ctrl+Z も iOS のシェイク取り消しも効かない。

### 対応

編集を1か所に集約し、取り消し履歴に載る方式へ変更する。

```js
// 選択範囲を text で置換する。ネイティブの undo 履歴に載せる
function replaceRange(ta, start, end, text) {
  ta.focus();
  ta.setSelectionRange(start, end);
  let ok = false;
  try {
    ok = document.execCommand('insertText', false, text);
  } catch (e) {
    ok = false;
  }
  if (!ok) {
    // フォールバック（undo 履歴は失われる）
    ta.value = ta.value.slice(0, start) + text + ta.value.slice(end);
    ta.selectionStart = ta.selectionEnd = start + text.length;
    ta.dispatchEvent(new Event('input'));
  }
}
```

- `heading` / `list` / `quote` / `code` / `bold` / `hr` / `tab` / Enter継続 をすべて `replaceRange()` 経由にする
- `execCommand('insertText')` は非推奨 API だが、textarea の取り消し履歴に編集を載せる代替手段が他にないため採用する。動かない環境では従来方式へ自動フォールバックする
- `input` イベントは execCommand が自動で発火するため、成功時は二重発火させない

## 2. リスト系キーと自動継続

### `-` キーの3状態トグル

見出しキーの連続押し（`app.js:1336-1352`）と同型で、行頭の状態を見て切り替える。

| 現在の行頭 | 押下後 |
|---|---|
| なし | `- ` |
| `- ` | `- [ ] ` |
| `- [ ] ` / `- [x] ` | 解除（マーカー削除） |

- インデント（先頭の空白）は保持する
- 挙動変更点: 現在の `-` キーはカーソル位置に `- ` を挿入するが、行頭基準に変更する（行の途中で押しても `abc- ` にならない）

### Enter の自動継続

現在の判定は `^(\s*[-*] )`（`app.js:1464`）のみ。以下へ拡張する。

| 行の形 | Enter 後 |
|---|---|
| `- item` / `* item` | `- ` を継続（現状どおり） |
| `- [ ] item` / `- [x] item` | `- [ ] ` を継続（チェック済みでも新規は未チェック） |
| `1. item` / `1) item` | 番号を +1 して継続 |
| 内容が空の項目 | マーカーを削除して解除（現状どおり） |

- 番号の振り直しは行わない（後続行の番号は変更しない）

## 3. コピーの改善

### 既定動作（タップ）

タイトル込み全文をコピーする。

```
title あり → "# {title}\n\n{body}"
title なし → "{body}"
```

### 長押しメニュー

コピーボタンを 500ms 長押しでメニューを開く。`contextmenu` は抑止する。

| 項目 | 内容 | リスト画面 | エディタ |
|---|---|---|---|
| タイトル込み全文 | 既定と同じ | ○ | ○ |
| 本文のみ | `body` のみ（現行の挙動） | ○ | ○ |
| このブロック | カーソル位置を含む `---` 区切り範囲 | — | ○ |
| 記号なし | Markdown 記号を落としたプレーンテキスト | ○ | ○ |

### 「このブロック」の範囲

カーソル位置から前後を走査し、`---` だけの行を境界とする。境界の `---` 自体は含めず、前後の空白を落とす。`---` が1つもなければ全文と同じ。

### 「記号なし」の変換規則

| 元 | 変換後 |
|---|---|
| `# 見出し` 〜 `###### 見出し` | `見出し` |
| `**強調**` / `*強調*` | `強調` |
| `` `コード` `` | `コード` |
| `> 引用` | `引用` |
| `- [ ] 項目` / `- [x] 項目` | `☐ 項目` / `☑ 項目` |
| `---` の行 | 空行 |
| `- 項目` | `- 項目`（リストは読みやすさのため残す） |

## 4. 検索

### 入口と表示

- リスト画面のヘッダーに検索アイコンを追加（メニューの左）
- タップするとタブ行の位置に検索バーが出て、タブは隠れる。input に自動フォーカス
- `✕` またはハードウェアの戻るで終了（`history.pushState({view:'search'})` を積む）

### 挙動

- 対象は `title` + `body`、大文字小文字を区別しない部分一致
- Inbox / Keep / Archive を横断し、各件にバケットのバッジを表示
- 入力のたびに再描画（インクリメンタル）。空クエリなら通常のリストへ戻す
- 並びは `updatedAt` 降順（ピン留めは考慮しない）
- プレビュー行はヒットした行に差し替え、一致部分をハイライトする（`innerHTML` は使わず `textContent` + `span` で構築）
- 結果のスワイプは無効。タップで開く。編集後に戻ると検索状態を維持する

---

## 変更対象ファイル

| ファイル | 内容 |
|---|---|
| `index.html` | 検索アイコン・検索バー、コピー形式メニュー |
| `app.js` | `replaceRange()` 導入と全編集の移行、`-` キー3状態、Enter 継続拡張、コピー3形式と長押し、検索 |
| `style.css` | 検索バー・バケットバッジ・ハイライト、コピーメニュー |
| `README.md` | コピー形式・検索の説明 |
| `VERIFY-v4.md`（新規） | 検証結果と手動チェックリスト |

## 実装ステップ

1. `replaceRange()` 導入、既存の編集処理を全移行（挙動は変えない）
2. `-` キー3状態トグル + Enter 継続拡張
3. コピー3形式 + 長押しメニュー
4. 検索
5. README / VERIFY-v4

## テスト方針

前回と同じ jsdom ハーネス（56 checks）を土台に、以下を追加して全パスを確認する。

- `replaceRange` のフォールバック経路（`execCommand` が無い環境）で従来どおり編集できる
- `-` キー3状態の遷移、インデント保持
- Enter 継続: `-` / `- [ ]` / `- [x]` / `1.` / `1)` / 空項目の解除
- コピー4形式の出力文字列（タイトル有無、ブロック境界、記号除去）
- 検索: 大文字小文字、横断、空クエリ復帰、ハイライト位置、ヒット0件

`execCommand` は jsdom に実装がないため、自動テストではフォールバック経路のみ検証できる。ネイティブ取り消しが実際に効くかは実機確認とする。

## ロールバック

コード変更のみでデータ形式は変えないため、`git revert` で完全に戻せる。

# QuickMemo v4 (コピー改善 / 編集操作修正 / 検索) - Verification

Date: 2026-07-27
対象: `replaceRange()` 導入、`-` キー3状態、Enter 継続拡張、コピー4形式、検索

---

## 1. 自動検証（jsdom ハーネス、117 checks / 全 PASS）

v3 の 56 checks に 61 checks を追加。全て PASS。

| # | 項目 | 件数 | 内容 |
|---|---|---|---|
| 1-10 | v3 既存分（移行・遷移・タブ・スワイプ・水平線・import/export 他） | 56 | 全て回帰なし |
| 11 | `-` キー3状態 | 7 | なし→`- `→`- [ ] `→なし / `- [x]` からも解除 / インデント保持 / 行中で押しても行頭に付与 / 2行目のみ変更 |
| 12 | Enter 継続 | 12 | `-` `*` 継続 / `- [ ]` `- [x]` は未チェックで継続 / `1.` `3)` インクリメント / インデント保持 / 空項目3種で解除 / 通常テキストは素通り |
| 13 | コピー4形式 | 16 | タイトル→`# T\n\nB` / タイトル空は本文のみ / 本文のみ / ブロック（先頭・中間・末尾・`---`行上・区切りなし・カーソルなし）/ 記号除去6項目 |
| 14 | 検索 | 20 | バー表示・タブとFAB退避 / 3バケット横断 / 新しい順 / バッジ / 大文字小文字無視 / ハイライト範囲 / スワイプ要素なし / 本文のみ一致 / 一致行をプレビュー表示 / 0件表示 / 空クエリ復帰 / 閉じる操作 |
| 15 | コピーメニュー | 6 | 初期非表示 / 開閉 / エディタでは「このブロック」表示、カーソルなしでは非表示 |

### 記号なしコピーの実測

| 入力 | 出力 |
|---|---|
| `# Head` | `Head` |
| `- item` | `- item`（維持） |
| `- [ ] todo` / `- [x] done` | `☐ todo` / `☑ done` |
| `> quote` | `quote` |
| `**bold** and *it* and \`code\`` | `bold and it and code` |
| `---` | 空行 |

## 2. 静的チェック

| 項目 | 結果 | 内容 |
|---|---|---|
| `node --check app.js` | PASS | 構文エラーなし |
| `innerHTML` 不使用 | PASS | 検索ハイライトも `textContent` + `span` で構築 |
| CSP | PASS | 変更なし |
| SW キャッシュ | PASS | `quickmemo-v8` → `quickmemo-v9` |
| `[hidden]` の効き | PASS | `display: flex` を持つ `.tabs` `.fab` に `[hidden] { display: none }` を追加（属性だけでは隠れないため） |

## 3. 実装上の判断と既知の制約

### `execCommand('insertText')` の採用

- 非推奨 API だが、textarea のネイティブ取り消し履歴に編集を載せる代替手段が他にない
- 失敗時（未実装・例外・`false` 返却）は従来の `ta.value` 代入へ自動フォールバックする。この場合は取り消し履歴が失われるが、編集自体は成立する
- **jsdom には `execCommand` の実装がないため、自動テストが通っているのはフォールバック経路のみ**。ネイティブ取り消しが実際に繋がるかは実機確認が必要（下記チェックリスト）

### 挙動変更

- `-` キーはカーソル位置挿入から**行頭基準**に変更。行の途中で押しても `abc- ` にならず、行頭に `- ` が付く
- コピーボタンのタップ結果が「本文のみ」から「タイトル込み全文」に変更

### スコープ外（PLAN-v4 非目標）

- Markdown プレビュー、Share / Share Target、検索結果からのスワイプ操作、タグ・並べ替え

## 4. 手動検証チェックリスト（実機・ブラウザ）

### 取り消し（最重要・自動検証不可）
- [ ] ツールバーの各キーを押した後、Ctrl+Z（PC）/ シェイク（iOS）で取り消せる
- [ ] Enter のリスト自動継続の後も取り消せる
- [ ] 取り消しを複数回続けても入力内容が壊れない
- [ ] 取り消し後の再入力（やり直し）が破綻しない

### リスト入力
- [ ] `-` キー連打で `- ` → `- [ ] ` → 解除 と循環する
- [ ] 日本語入力（IME 変換中）に Enter を押しても継続処理が誤発火しない
- [ ] 番号付きリストで `1.` → `2.` → `3.` と続く

### コピー
- [ ] リストのコピーボタンをタップ → タイトル込み全文
- [ ] 長押し → メニュー表示、選択後に正しい内容がコピーされる
- [ ] 長押し後に指を離しても通常コピーが二重に走らない
- [ ] 長押し中に OS 標準のコンテキストメニューが出ない
- [ ] エディタで「このブロック」がカーソル位置の `---` 区間だけになる
- [ ] メニュー表示位置が画面外にはみ出さない（狭い端末幅・最下部のメモで確認）

### 検索
- [ ] 検索アイコン → バー表示、キーボードが自動で出る
- [ ] 入力に応じて結果が即時更新される
- [ ] Inbox / Keep / Archive のメモが混ざって出て、バッジが正しい
- [ ] ハイライトが一致箇所とずれない（日本語・記号を含む語で確認）
- [ ] 結果からメモを開いて戻ると検索語が保持されている
- [ ] 検索中に Archive を開いて戻っても表示が壊れない
- [ ] `✕` と端末の戻る操作の両方で検索が閉じる

### 回帰
- [ ] スワイプ8パターンと Undo（v3 の機能）
- [ ] 音声メモ・追記
- [ ] Export / Import
